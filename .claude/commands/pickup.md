---
description: Orient a fresh session on the live Abitzu Lead Pipeline before any work
---

Read `PICKUP.md` at the repository root **in full** and treat it as your operating
brief for this session.

Then complete its Orientation Checklist before proposing or making ANY changes:

1. Read `index.html` and decode the embedded template string (the app's real source
   lives there) — understand the `Component` class and the HTML template.
2. Verify the Supabase connection with the Supabase MCP tools (`list_projects`,
   `list_tables`) and report the tables, row counts, RLS status, triggers, and that
   realtime is on.
3. Find the current version with the GitHub MCP tools (repo, latest commit, open or
   merged PRs on the working branch).
4. Confirm the Vercel-served `index.html` matches this repo.
5. Post a short **"state of the world"** summary and confirm with me before editing.

I may not be technical — explain findings in plain language and translate my
requests into the actual work yourself. When you're oriented, ask me what I'd like
to enhance.
